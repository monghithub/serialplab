-- Create schemas for Java services (Go and Node create their own)
-- Hibernate ddl-auto=update cannot create schemas, only tables

CREATE SCHEMA IF NOT EXISTS springboot;
CREATE SCHEMA IF NOT EXISTS quarkus;
